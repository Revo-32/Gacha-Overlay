# Core M5 — 판매·세션 네이티브 표시

상태: **CONDITIONAL PASS — 합성 표시 검증 완료, 실시간/상호작용 동등성 gate 남음**.

- Backend의 기존 Sales domain과 Full `SalesQueuePresentationFactory`를 재사용한다. Core 전용 opt-in DTO에 의미적 headline/상태/색상 종류를 추가했으며, Full DTO와 운영 route는 바꾸지 않았다.
- 현재/다음 차례, 자기 차례 강조, 복구 중 Last-Good 표시, 완료 후 다음 판매자 승격, 빈 Live 대기열 숨김을 구현했다. C++에는 SOLD/closed 해석기가 없다.
- 왼쪽 정렬·세로 중앙 headline, 접기/펼치기, 별도 상세 스크롤, 상품/개수/현재·다음·나 표시, 판매 상세 custom emoji를 렌더링한다. Chat/판매는 같은 미디어 decoder/pixel/bitmap과 visibility 집합을 공유한다. 실패 이모지는 원래 이름으로 되돌린다.
- 서버 HostPresence의 인원/미확인/오프라인/다른 게임 상태를 구분하고 호스트 선택을 제공한다. 미확인을 0명으로 표시하지 않는다. GTA 메모리 읽기는 없다.
- 검증 경로는 명시적 읽기 전용이다. `CompletionEnabledMessageIds`는 기본 빈 목록이며 현재 native UI에 실제 판매 변경 버튼/전송 경로를 넣지 않았다. **이것을 Full의 판매 완료 조작까지 동등하다고 판단하지 않는다.**

## 검증

- 집중 관리 코드 검사: `CoreSalesM5Tests | CoreProtocolM2Tests` 18 tests PASS.
- Release native build와 CTest 5/5 PASS.
- 합성 native GUI 17 assertions PASS: 3개 queue, 현재/다음, 접기 중 데이터 유지, 잠금 중 클릭 거부, recovery 유지, 완료 후 2개, 빈 queue 숨김, 12/30·미확인·오프라인 상태. 이미지로 정렬/줄바꿈/custom emoji 렌더링 확인.
- 자체 검증 창만 생성·종료했으며 Full 단축키/설정/프로세스와 Discord 상태는 변경하지 않았다.
- 실제 접속/10분 성능/Full 동일 workload 비교는 M6 별도 gate다. 합성 숫자를 실환경 성능으로 보고하지 않는다.

## 재현

`tools/core/LSOverlay.CoreFixtureHost --export-sales <절대 artifact 경로>`로 합성 next/current/recovering/completed/empty 파일을 생성한다. native `--no-hotkeys --chat-fixture <next.json> --sales-verify <새 절대 경로>`는 약 5초 후 자체 종료한다. M4의 합성 `--media-fixture <media.json>`을 함께 지정하면 판매 상세 이모지도 렌더링한다.

남은 사항: 실제 인증된 stream, 실제 판매 변경 조작/공식 read-back 확인, Full의 상대 시간·전환 애니메이션·설정 동등성, M6 통합 품질/안정성. M7 패키징 승인은 아직 아니다.
