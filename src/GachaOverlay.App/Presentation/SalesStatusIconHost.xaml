<UserControl x:Class="GachaOverlay.App.Presentation.SalesStatusIconHost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:GachaOverlay.App.Presentation"
             Width="28"
             Height="28"
             Focusable="False"
             IsHitTestVisible="False"
             SnapsToDevicePixels="True"
             UseLayoutRounding="True">
    <UserControl.Resources>
        <local:BooleanToVisibilityConverter x:Key="BoolVisibility" />
    </UserControl.Resources>
    <Grid x:Name="SafeIconSurface" Width="28" Height="28" ClipToBounds="False">
        <Ellipse Width="8"
                 Height="8"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Center"
                 Fill="{DynamicResource StatusLiveBrush}"
                 Visibility="{Binding IsLiveIndicatorVisible, Converter={StaticResource BoolVisibility}}" />

        <Ellipse x:Name="SpinnerRing"
                 Width="16"
                 Height="16"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Center"
                 Stroke="{DynamicResource StatusInfoBrush}"
                 StrokeDashArray="2,2"
                 StrokeThickness="2"
                 RenderTransformOrigin="0.5,0.5"
                 Visibility="{Binding IsSpinnerVisible, Converter={StaticResource BoolVisibility}}">
            <Ellipse.RenderTransform>
                <RotateTransform x:Name="SpinnerRotation" />
            </Ellipse.RenderTransform>
        </Ellipse>

        <Viewbox Width="18" Height="18"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Center"
                 Stretch="Uniform"
                 Visibility="{Binding IsWarningVisible, Converter={StaticResource BoolVisibility}}">
            <Grid Width="18" Height="18">
                <Path Data="M 9,1 L 17,17 L 1,17 Z" Fill="{DynamicResource StatusWarningBrush}" />
                <Path Data="M 8.15,5 L 9.85,5 L 9.6,11 L 8.4,11 Z M 8.1,13 L 9.9,13 L 9.9,14.8 L 8.1,14.8 Z" Fill="{DynamicResource ChatOutlineBrush}" />
            </Grid>
        </Viewbox>

        <Viewbox Width="18" Height="18"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Center"
                 Stretch="Uniform"
                 Visibility="{Binding IsErrorVisible, Converter={StaticResource BoolVisibility}}">
            <Grid Width="18" Height="18">
                <Ellipse Width="17" Height="17" HorizontalAlignment="Center" VerticalAlignment="Center" Fill="{DynamicResource StatusErrorBrush}" />
                <Path Data="M 8.15,4 L 9.85,4 L 9.6,11 L 8.4,11 Z M 8.1,13 L 9.9,13 L 9.9,14.8 L 8.1,14.8 Z" Fill="{DynamicResource ChatOutlineBrush}" />
            </Grid>
        </Viewbox>
    </Grid>
</UserControl>
